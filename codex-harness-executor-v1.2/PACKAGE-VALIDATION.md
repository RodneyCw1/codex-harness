# v1.2.1 发行包

常规回归 97 项、Windows 宿主 8 项、TypeScript 和编译通过。详见 VALIDATION-V1.2.1.md 及 reports。真实模型本次未执行。

执行器已经编译，不含 Node.js 运行时；新电脑使用 Node.js 24，继续配合 v1.2 项目规范模板。没有加入业务项目、私有配置、密钥、node_modules 或临时运行状态。SHA256SUMS.txt 覆盖包内文件。
