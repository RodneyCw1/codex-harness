# 验证记录

当前版本为 **1.2.1**。本次原有 63 项及新增 34 项回归合计 97 项通过，Windows 宿主 8 项通过，TypeScript 与构建通过。真实模型本次未运行。

详细范围、原始日志与限制见 [VALIDATION-V1.2.1.md](VALIDATION-V1.2.1.md)。下面保留 v1.2 的历史验证；不将历史真实模型联调当成本次执行结果。

## v1.2 历史验证

日期：2026-09-23。测试机 Windows，Node.js 24.21.0，Codex CLI 0.155.0-alpha.16。

- 规则、模拟模型服务及真实临时文件回归：63 项通过，0 失败、0 跳过。包含原 34 项、输入篡改、必需报告、生成目录、锁与状态恢复、规范依赖失效、运行时漂移及日志分页。
- Windows 原生宿主：目录与网络预期探针、子进程停止 2 项通过；大量文档目录保护参数回归1项通过；真实私有环境文件假密钥、junction 与工作 AI 文件工具 3 项通过。原生沙箱测试由宿主启动，外层受限会话中的嵌套启动曾超时，已停止相关测试进程后从宿主复验。
- TypeScript 检查通过；已编译 CLI 的 doctor 通过。软件安装后还要核对成品版本和项目 doctor。
- 真实服务工具往返通过。独立加法项目第一轮保留故障注入的错误实现，独立检查实际 6 != 5，Codex REVISE；第二轮工作 AI 调用 apply_patch 修复为 a+b；独立四项断言通过，Codex ACCEPT；FINAL 重新验证四项通过并 export。
- 实际最终 run：run-f868df00-e216-4818-805c-6ee94635f645；候选 sha256:512cbf5be10eae093bae7c76d8282cce9b9f9b4b0fd32cd7f5b112a1ea51dffe。

这证明该机器、该模型配置及测试项目的闭环可运行，不代表任意项目的业务验收已经通过。当前用户项目的 Maven 基线另存项目接入报告；没有测试用例、数据库/Redis 或用户场景运行证据的部分不得标为通过。

本版 trusted_local 是显式选择的可信本机模式，允许普通文件读取和联网；指定敏感目录拒绝访问，写入与验收文件仍受限制。不承诺全面读取隔离或断网，不修改防火墙。旧 strict 配置保持原有语义，在当前 CLI 不支持时明确阻塞。

首次原版复现结果：独立新增完整性测试 7 项中 5 项失败，初始化恢复用例 3 项失败；修复后全部通过。活跃旧版锁被误删的迁移边界也先复现失败，再修复并验证。

reports/regression-v1.2.txt、host-v1.2.txt、host-v12-*-boundary.json 为测试证据；live-* 为独立联调记录。旧版本历史报告可能记录失败，不适用于当前模式。

正式项目接入实测：2026-09-23，Maven verify 根项目与8个子模块构建成功，未发现测试用例（0），业务验收未执行。接入还复现并修复 Windows 受保护文档参数过长及正斜杠 cmd.exe 路径转义问题。相关原始项目日志留项目控制目录，不包含在通用模板包中。
