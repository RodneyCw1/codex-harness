# 会话交接

状态：待接入。不存在预设项目名、运行 ID、批次或通过记录。

接入及每次需要交接时，由 Codex 记录当前工作树、本机配置位置、需求与规范版本、已完成且有证据的工作、未验证改动、阻塞和恢复条件。所有秘密值留在私有文件。

新电脑只有仓库时可开始新任务；没有完整控制数据与候选，不能冒称恢复旧运行。已有运行先 status，普通中断保留原计数，明确续跑预算暂停才创建新批次。
