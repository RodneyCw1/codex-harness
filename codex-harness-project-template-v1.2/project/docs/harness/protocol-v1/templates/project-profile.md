# 项目画像

状态：草稿，待完成预检。填写日期：[日期]。维护者：Codex。

## 项目事实

| 项目 | 内容与依据 |
|---|---|
| 项目名称、用途、目标用户 | [填写] |
| 仓库绝对路径、Git 根或非 Git 标识 | [填写] |
| 技术栈、运行时版本、包管理器 | [文件及版本依据] |
| 有效项目指令 | [路径、适用范围] |
| 架构、产品、编码与测试规范 | [路径、相关章节] |
| CodeGraph 状态 | [存在/不存在；查询方式；不可用原因] |
| 需求涉及模块、入口和现有相似实现 | [路径、符号与理由] |

## 基线与隔离

- Git commit（如有）：[值]
- 内容快照及清单引用：[值，包含未跟踪但需要的文件]
- 用户未提交改动：[清单；与需求的关系；保留方式]
- 独立执行工作区：[绝对路径]
- 受信运行存储：[绝对路径；执行者不可写]
- 验证环境：[操作系统、运行时、依赖锁、服务、测试数据]
- 现有验证结果：[检查、日志引用、退出码、失败归因]
- 明确无关的基线问题：[证据、Codex决定；不得排除必需AC]
- 阻塞及解除条件：[没有则填“无”]

## 项目命令注册表

| command_id | argv 数组 | cwd（根相对） | 超时秒 | 用途/预期结果 | 是否可重复执行 |
|---|---|---|---|---|---|
| [ID] | [参数数组] | [目录] | [数值] | [初始化/测试/静态检查/构建] | [是/否与依据] |

初始化、构建和测试均使用实际项目命令。长驻服务还要记录启动、健康检查与停止方式；命令表最终与配置保持一致。

## 边界

- 允许读取的上下文：[明确路径]
- 允许修改的实现与测试：[路径或目录前缀]
- 受保护规范、测试和运行文件：[明确路径]
- 已授权网络目标及用途：[模型服务、必要依赖或测试服务]
- 命令可继承的非秘密环境变量：[名称列表]
- 测试凭据或资源要求：[名称和用途，不填写秘密值]
