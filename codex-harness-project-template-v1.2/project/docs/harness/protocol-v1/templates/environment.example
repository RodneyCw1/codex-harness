# 变量名示例，不是可直接运行的 shell 脚本。
# 在受信的执行器环境中设置，不能把真实密钥提交到仓库。
EXECUTOR_BASE_URL=https://provider.example/v1
EXECUTOR_API_KEY=
EXECUTOR_MODEL=REPLACE_MODEL_ID
PROJECT_ROOT=REPLACE_ABSOLUTE_PROJECT_PATH
HARNESS_WORK_ROOT=REPLACE_ABSOLUTE_ISOLATED_WORK_PATH
HARNESS_CONTROL_ROOT=REPLACE_ABSOLUTE_TRUSTED_CONTROL_PATH
