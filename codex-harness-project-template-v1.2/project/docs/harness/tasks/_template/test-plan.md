---
task_id: REPLACE_TASK_ID
spec_version: 1
---
# 测试文档

任务 ID：[TASK-ID] · 规范版本：[整数]

## 测试环境和基线

- 环境标识、运行时、依赖锁：[填写]
- 初始化、启动、健康检查与停止命令：[command_id]
- 基线结果及现有问题：[证据引用]
- 数据构造、清理及外部服务：[填写]
- 所需凭据名称：[名称与用途，不能填值]

## 测试用例

| TC ID | AC ID | 层级 | 前置/数据 | 动作或command_id | 预期断言 | 必须保留的证据 |
|---|---|---|---|---|---|---|
| TC-001 | AC-001 | [单元/集成/E2E/检查/人工观察] | [填写] | [填写] | [填写] | [填写] |

按需求覆盖成功路径、错误输入、边界和关联回归；只编写有实际判别力的用例。不适用的测试层级在此说明理由，不留到失败后再排除。

## 官方验证入口

- 执行者自测 command_id：[列表]
- Codex 独立验收 command_id：[列表及顺序]
- 受保护验收测试/脚本：[路径与摘要]
- 最终快照回归/集成 command_id：[列表及覆盖的AC]
- 通过条件：[退出码、断言、启动健康及场景结果]
- 超时或环境错误处理：标记未验证并BLOCK，不能跳过后通过。

## 证据约定

每项保存任务、功能、批次、候选提交、代码快照、规范版本与摘要、环境标识、命令参数、退出码、时间和日志/产物摘要。多个测试合并运行时，日志必须能分别定位各AC/TC结果。

验证前冻结候选，验证后确认内容未改变。构建输出与日志放在快照输入之外；测试运行改写源文件时证据失效，须重新冻结。

## 失败与复验

产品失败形成返工单；环境、配置及权限失败形成阻塞。修复后重跑失败检查和受影响回归；最终交付必须在最终快照执行本节规定的整体检查。
