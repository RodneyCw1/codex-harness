# 发行包验证

本版模板配合执行器 1.3.0，配置为 1.3，目录名保留 v1.2；固定 1.0 协议原件及其 62 份文件摘要保持不变。模板包含文档版本元数据、测试计数配置和自动接入说明。

本版实际验证见执行器 `VALIDATION-V1.3.md` 及 `reports/*v1.3*`。真实供应商联调本版未执行，模拟供应商结果单独记录。原 `VALIDATION.md` 是历史验证，不代表本版已经重做真实供应商联调。新电脑须自行运行 `doctor --live` 和项目基线；模板不含项目验收结论。
